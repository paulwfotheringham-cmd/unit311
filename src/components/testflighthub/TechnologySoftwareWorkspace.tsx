"use client";

import SoftwareAssetRegisterWorkspace from "./SoftwareAssetRegisterWorkspace";

/**
 * Technology Management — Software
 * Hosts the existing Software Licences / SaaS register (moved from Corporate Information).
 */
export default function TechnologySoftwareWorkspace() {
  return <SoftwareAssetRegisterWorkspace />;
}
