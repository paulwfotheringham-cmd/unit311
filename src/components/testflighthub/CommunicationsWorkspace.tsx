"use client";

/**
 * Communications hub — unified messaging, voice, and video for Unit311 Central.
 * Implementation lives in MessagingWorkspace (preserved APIs / call stack).
 */
export { default } from "./MessagingWorkspace";
